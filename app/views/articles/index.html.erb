<h1>Articles</h1>
<% @articles.each do |article| %>
    <article>
      <h2><%= article.name %></h2>
      <p><%= article.content[0..40] %></p>
      <%= link_to 'Read more', "/articles/#{article.id}" %>
    </article>
<% end %>
